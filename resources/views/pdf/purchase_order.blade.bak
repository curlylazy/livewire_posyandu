@use('Illuminate\Support\Facades\Vite')

<!doctype html>
<html lang="en">
    <head>
        <!-- Required meta tags -->
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <title>Cetak Bukti PO</title>
        <link href="{{ resource_path('assets/pdf/table.css') }}" rel="stylesheet" type="text/css">
        <link href="{{ resource_path('assets/pdf/style.css') }}" rel="stylesheet" type="text/css">

        <style>
            @font-face {
                font-family: 'sans_serif';
                font-style: normal;
                font-weight: 400;
                src: url('{{ storage_path('fonts/sans_serif.ttf') }}') format('truetype');
            }

            @font-face {
                font-family: 'roboto';
                font-style: normal;
                font-weight: 400;
                src: url('{{ storage_path('fonts/roboto.ttf') }}') format('truetype');
            }

            body {
                font-family: 'roboto';
                font-size: 11pt;
            }
        </style>
    </head>
    <body>

        @include('pdf.watermark')
        @include('pdf.header')

        <div style="float: left">
            <h4 style="margin-bottom: 0px; font-weight: bold"><strong>Tagihan Kepada</strong></h4>
            <div >
                <h3 style="margin-bottom: 0px"><strong>{{ $dataPesanHd->namapelanggan }}</strong></h3>
                {{ $dataPesanHd->alamat }}<br>
                Email : {{ $dataPesanHd->email }}<br>
                No Hp : {{ $dataPesanHd->nohp }}<br>
            </div>
        </div>

        <div style="float: right; text-align: right;">
            <h4 style="margin-bottom: 0px; font-weight: bold"><strong>Invoice</strong></h4>
            <div >
                <h3 style="margin-bottom: 0px"><strong>Invoice #{{ Str::replace('INVOICE', '', $dataPesanHd->noinvoice) }}</strong></h3>
                Tanggal : {{ Carbon\Carbon::parse($dataPesanHd->created_at)->format('d F Y') }} <br />
                Dikirim : {{ Carbon\Carbon::parse($dataPesanHd->tgl_pengiriman)->format('d F Y') }} <br />
                Status Pesan : {{ ($dataPesanHd->statuslunas) ? 'Lunas' : 'Belum' }} <br />
            </div>
        </div>

        <div style="clear: both;"></div>

        <table class="greyGridTable" style="margin-top: 20px;">
            <thead>
                <tr>
                    <th scope="col">Produk</th>
                    <th scope="col">Jenis Kain</th>
                    <th scope="col">Warna</th>
                    <th scope="col">Ukuran</th>
                    <th scope="col">Qty</th>
                    <th scope="col">Harga</th>
                    <th scope="col">Subtotal</th>
                </tr>
            </thead>
            <tbody>
                @foreach ($dataPesanDt as $data)
                    <tr>
                        <td>{{ $data->namaproduk }}</td>
                        <td>{{ $data->namajeniskain }}</td>
                        <td>{{ $data->warna }}</td>
                        <td>{{ $data->ukuran }}</td>
                        <td>{{ $data->qty }}</td>
                        <td>Rp{{ number_format($data->hargapokok) }}</td>
                        <td>Rp{{ number_format($data->hargapokok * $data->qty) }}</td>
                    </tr>
                @endforeach

                <tr>
                    <td scope="row" colspan="6" class="text-uppercase text-end">Total</td>
                    <td class="text-end">Rp{{ number_format($dataPesanHd->totalbayar) }}</td>
                </tr>
            </tbody>
        </table>
    </body>
</html>
